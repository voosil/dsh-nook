# 必填：其他设备实际访问的 IP 或域名，不带协议、路径和端口。
NOOK_HOST=
NOOK_MODE=vpn
NOOK_PORT=8443
# 公网 IP：NOOK_MODE=ip，可使用 443。
# 公网域名：NOOK_MODE=domain，NOOK_PORT=443，填写邮箱并接受 ACME 条款，
# 启动时额外使用 -f compose.domain.yaml。
NOOK_EMAIL=
NOOK_ACCEPT_ACME_TERMS=false
# 默认发布到服务器全部 IPv4 接口；也可填写指定的内网接口地址。
NOOK_BIND_IP=0.0.0.0
